import React, { PureComponent,Fragment } from 'react';
import { WriterWrapper } from '../Homes.js';

class Writer extends PureComponent{
    render(){
        return(
            <Fragment>
                <WriterWrapper>Writer</WriterWrapper>
            </Fragment>
        )
    }
}
export default Writer;
